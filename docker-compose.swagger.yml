version: '3.9'

services:
  swagger-ui:
    image: swaggerapi/swagger-ui:v5.17.14
    container_name: swagger-ui
    ports:
      - "8088:8080"
    environment:
      SWAGGER_JSON: /tmp/openapi.yaml
    volumes:
      - ./deploy/swagger/openapi.yaml:/tmp/openapi.yaml:ro
    restart: unless-stopped
